/**
 * App constants: API base URL, feature flags, etc.
 * Replace with env (e.g. EXPO_PUBLIC_API_URL) when backend exists.
 */

export const API_BASE_URL = process.env.EXPO_PUBLIC_API_URL ?? "https://api.example.com";
export const MAX_IMAGE_SIZE_KB = 2048;
export const MAX_IMAGE_DIMENSION = 1920;
